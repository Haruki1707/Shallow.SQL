using System;
using System.Collections.Generic;
using System.Text;

namespace Shallow.SQL.Enums
{
    public enum Relations
    {
        hasOne,
        hasMany,
        belongsTo,
        belongsToMany
    }
}
